GEMINI's qualification candidate still had gaps in input coverage, restart precision/state, optional-mode enforcement and current-dependency verification. A newly merged upstream background-initialization change also made frozen-neutral restart trajectories depend on the restart time.

This continuation merges upstream main at 9320b891 and retains the configured density-floor correction. It rejects uncovered or invalid forcing by default, adds stream preflight, stores float64 core restart fields and full potential, validates checkpoint schema and actual precision, and preserves the configured epoch for frozen backgrounds. It corrects a curved-gradient boundary metric index and adds midpoint diffusion boundary data. Capacitance is restricted to the implemented 3D Cartesian field-integrated gradient-boundary formulation, preventing nonfinite-current failures in unsupported combinations.

Qualification now includes manufactured convergence/source-boundary checks, explicit physical-volume budget tools, an independently implemented IGRF-14 oracle compared with official code, C17 and C++ consumer checks, GNU sanitizers, optional-mode smoke experiments, a verified HDF5 2.2.0 build profile, inventories and additional hosted CI jobs. An isolated native-test output root supports repeatable tests in synchronized workspaces. Reference data and tolerances are unchanged.

See the accompanying verification manifest for exact local results and candidate identity. Hosted jobs have not run remotely. The 18-gate register explicitly retains application decisions, complete conservation and restart-state work, independent physics and optional-mode benchmarks, target-platform/security/license approval and later integration work. No IGRF-14 production-grid, operational, surrogate, control or coupled-model qualification is asserted.
