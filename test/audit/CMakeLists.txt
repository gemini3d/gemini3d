# Audit additions 2026-09-16: targeted native regressions, Apache-2.0.
add_executable(audit_cli cli_test.cpp)
target_include_directories(audit_cli PRIVATE ${PROJECT_SOURCE_DIR}/app ${PROJECT_SOURCE_DIR}/include)
add_test(NAME audit:cli COMMAND audit_cli)
add_executable(audit_sanity sanity_probe.f90)
target_link_libraries(audit_sanity PRIVATE sanity_check errors const h5fortran::h5fortran)
add_executable(audit_config config_probe.f90)
target_link_libraries(audit_config PRIVATE gemini3d_config const timeutils gemini3d_sysinfo ffilesystem::filesystem)
if(CMAKE_Fortran_COMPILER_ID MATCHES "^Intel")
  set_property(TARGET audit_config PROPERTY LINKER_LANGUAGE Fortran)
else()
  set_property(TARGET audit_config PROPERTY LINKER_LANGUAGE CXX)
endif()
function(intel_fortran_main_linker target_name)
  if(CMAKE_Fortran_COMPILER_ID MATCHES "^Intel")
    set_property(TARGET ${target_name} PROPERTY LINKER_LANGUAGE Fortran)
  endif()
endfunction()
add_executable(audit_interp interp_test.f90)
target_link_libraries(audit_interp PRIVATE interp const)
add_test(NAME audit:interpolation COMMAND audit_interp)
add_executable(audit_pole pole_probe.f90)
target_link_libraries(audit_pole PRIVATE geomagnetic const)
add_test(NAME audit:unsupported_pole_year COMMAND audit_pole)
set_tests_properties(audit:unsupported_pole_year PROPERTIES WILL_FAIL TRUE)
set_tests_properties(audit:cli audit:interpolation audit:unsupported_pole_year PROPERTIES LABELS "unit;audit")

add_executable(audit_time time_probe.f90)
target_link_libraries(audit_time PRIVATE timeutils const)
add_executable(audit_coordinates coordinate_test.f90)
target_link_libraries(audit_coordinates PRIVATE geomagnetic const)
add_test(NAME audit:coordinates COMMAND audit_coordinates)
add_executable(audit_cfl cfl_probe.f90)
target_link_libraries(audit_cfl PRIVATE temporal const)
add_test(NAME audit:cfl COMMAND audit_cfl)
foreach(mode IN ITEMS nan metric temperature step)
  add_test(NAME audit:cfl_reject_${mode} COMMAND audit_cfl ${mode})
  set_tests_properties(audit:cfl_reject_${mode} PROPERTIES WILL_FAIL TRUE LABELS "unit;audit")
endforeach()
add_executable(audit_cabi cabi_test.cpp cabi_size.f90)
target_include_directories(audit_cabi PRIVATE ${PROJECT_SOURCE_DIR}/include)
target_link_libraries(audit_cabi PRIVATE gemini3d_c gemini3d)
add_test(NAME audit:cabi COMMAND audit_cabi)
add_test(NAME audit:cabi_unsupported_config COMMAND audit_cabi unsupported)
set_tests_properties(audit:cabi_unsupported_config PROPERTIES WILL_FAIL TRUE)
set_tests_properties(audit:coordinates audit:cfl audit:cabi audit:cabi_unsupported_config PROPERTIES LABELS "unit;audit")
if(Python_Interpreter_FOUND)
  add_test(NAME audit:calendar COMMAND ${Python_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/test_calendar.py
    --exe $<TARGET_FILE:audit_time> --output ${CMAKE_CURRENT_BINARY_DIR}/calendar-results.json)
  add_test(NAME audit:native_probes COMMAND ${Python_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/run_native_probes.py
    --build ${PROJECT_BINARY_DIR} --output ${CMAKE_CURRENT_BINARY_DIR}/native-probes.json)
  set_tests_properties(audit:calendar audit:native_probes PROPERTIES TIMEOUT 120 LABELS "unit;audit")
endif()

add_library(audit_driver_fixture driver_fixture.f90)
target_link_libraries(audit_driver_fixture PRIVATE inputdata gemini3d)
add_executable(audit_driver driver_test.f90)
target_link_libraries(audit_driver PRIVATE audit_driver_fixture inputdata meshobj_cart meshobj_dipole timeutils gemini3d)
intel_fortran_main_linker(audit_driver)
add_test(NAME audit:driver_midnight COMMAND audit_driver)
set_tests_properties(audit:driver_midnight PROPERTIES LABELS "unit;audit")

add_executable(audit_mpi_time mpi_time_test.f90)
target_link_libraries(audit_mpi_time PRIVATE temporal_mpi temporal meshobj_cart mpimod gemini3d MPI::MPI_Fortran)
intel_fortran_main_linker(audit_mpi_time)
foreach(mode IN ITEMS normal worker_cfl)
  add_test(NAME audit:mpi_time_${mode}
    COMMAND ${MPIEXEC_EXECUTABLE} ${MPIEXEC_NUMPROC_FLAG} 2 ${MPIEXEC_PREFLAGS}
      $<TARGET_FILE:audit_mpi_time> ${MPIEXEC_POSTFLAGS} ${mode})
  set_tests_properties(audit:mpi_time_${mode} PROPERTIES TIMEOUT 30 PROCESSORS 2 LABELS "unit;audit;mpi")
endforeach()
set_tests_properties(audit:mpi_time_worker_cfl PROPERTIES WILL_FAIL TRUE)

add_executable(audit_density_floor density_floor_test.f90)
target_link_libraries(audit_density_floor PRIVATE multifluid meshobj_cart grid const gemini3d)
intel_fortran_main_linker(audit_density_floor)
add_test(NAME audit:density_floor COMMAND audit_density_floor)
set_tests_properties(audit:density_floor PROPERTIES LABELS "unit;audit")

add_executable(qualification_coverage coverage_test.f90)
target_link_libraries(qualification_coverage PRIVATE audit_driver_fixture inputdata gemini3d)
intel_fortran_main_linker(qualification_coverage)
foreach(mode IN ITEMS covered flagged reject nan)
  add_test(NAME qualification:coverage_${mode} COMMAND qualification_coverage ${mode})
  set_tests_properties(qualification:coverage_${mode} PROPERTIES LABELS "unit;qualification")
endforeach()
set_tests_properties(qualification:coverage_reject qualification:coverage_nan PROPERTIES WILL_FAIL TRUE)
add_executable(qualification_diffusion diffusion_convergence.f90)
target_link_libraries(qualification_diffusion PRIVATE PDEparabolic const MPI::MPI_Fortran)
add_test(NAME qualification:diffusion_convergence COMMAND qualification_diffusion)
set_tests_properties(qualification:diffusion_convergence PROPERTIES LABELS "unit;qualification" TIMEOUT 60)

if(Python_Interpreter_FOUND)
  add_executable(qualification_igrf_reference ../qualification/igrf_reference.f90 ../qualification/data/igrf14syn.f)
  # Isolate the official legacy source from GEMINI's implicit-none policy.
  if(CMAKE_Fortran_COMPILER_ID STREQUAL "GNU")
    target_compile_options(qualification_igrf_reference PRIVATE -fno-implicit-none -Wno-error=line-truncation)
  endif()
  # The official routine is retained verbatim, including its legacy labeled DO.
  add_test(NAME qualification:igrf14 COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_igrf.py
    --exe $<TARGET_FILE:qualification_igrf_reference>
    --output ${CMAKE_CURRENT_BINARY_DIR}/igrf14-results.json)
  set_tests_properties(qualification:igrf14 PROPERTIES LABELS "unit;qualification" TIMEOUT 60)
endif()

if(NUMPY_FOUND AND H5PY_FOUND)
  add_test(NAME qualification:budgets COMMAND ${Python_EXECUTABLE} ${PROJECT_SOURCE_DIR}/test/qualification/test_budgets.py)
  set_tests_properties(qualification:budgets PROPERTIES LABELS "unit;qualification")
  add_test(NAME qualification:driver_preflight COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_driver_preflight.py)
  set_tests_properties(qualification:driver_preflight PROPERTIES LABELS "unit;qualification")
endif()

add_executable(qualification_capacitance capacitance_mode.f90)
target_link_libraries(qualification_capacitance PRIVATE potential PDEelliptic meshobj_cart gemini3d)
intel_fortran_main_linker(qualification_capacitance)
foreach(mode IN ITEMS supported 2d field_resolved dirichlet current_bc curved nan)
  add_test(NAME qualification:capacitance_${mode} COMMAND qualification_capacitance ${mode})
  set_tests_properties(qualification:capacitance_${mode} PROPERTIES LABELS "unit;qualification")
  if(NOT mode STREQUAL "supported")
    set_tests_properties(qualification:capacitance_${mode} PROPERTIES WILL_FAIL TRUE)
  endif()
endforeach()
add_executable(qualification_gradient gradient_metric.f90)
target_link_libraries(qualification_gradient PRIVATE calculus meshobj_cart const gemini3d)
intel_fortran_main_linker(qualification_gradient)
add_test(NAME qualification:gradient_metric COMMAND qualification_gradient)
set_tests_properties(qualification:gradient_metric PROPERTIES LABELS "unit;qualification")
if(Python_Interpreter_FOUND)
  add_test(NAME qualification:gate_integrity COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_gates.py)
  set_tests_properties(qualification:gate_integrity PROPERTIES LABELS "unit;qualification")
endif()

add_executable(qualification_c_consumer c_consumer.c cabi_size.f90)
target_include_directories(qualification_c_consumer PRIVATE ${PROJECT_SOURCE_DIR}/include)
target_compile_features(qualification_c_consumer PRIVATE c_std_17)
target_link_libraries(qualification_c_consumer PRIVATE gemini3d_c gemini3d)
set_property(TARGET qualification_c_consumer PROPERTY LINKER_LANGUAGE CXX)
add_test(NAME qualification:c_consumer COMMAND qualification_c_consumer)
set_tests_properties(qualification:c_consumer PROPERTIES LABELS "unit;qualification")

add_executable(qualification_atomic ../qualification/atomic_probe.f90)
target_link_libraries(qualification_atomic PRIVATE atomic_file)
add_executable(qualification_transport ../qualification/transport_convergence.f90)
target_link_libraries(qualification_transport PRIVATE advec transport_audit const gemini3d)
intel_fortran_main_linker(qualification_transport)
add_test(NAME qualification:transport_convergence COMMAND qualification_transport)
set_tests_properties(qualification:transport_convergence PROPERTIES LABELS "unit;qualification")
if(Python_Interpreter_FOUND)
  add_test(NAME qualification:atomic_checkpoint COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_atomic.py --exe $<TARGET_FILE:qualification_atomic>)
  set_tests_properties(qualification:atomic_checkpoint PROPERTIES LABELS "unit;qualification")
endif()
add_executable(qualification_size_alias ../qualification/size_alias.f90)
target_link_libraries(qualification_size_alias PRIVATE reader h5fortran::h5fortran const)
intel_fortran_main_linker(qualification_size_alias)
add_test(NAME qualification:driver_size_alias COMMAND qualification_size_alias ${CMAKE_CURRENT_BINARY_DIR}/size-alias.h5)
set_tests_properties(qualification:driver_size_alias PROPERTIES LABELS "unit;qualification")
if(NUMPY_FOUND AND H5PY_FOUND)
  add_test(NAME qualification:research_profile COMMAND ${Python_EXECUTABLE} ${PROJECT_SOURCE_DIR}/test/qualification/test_profile.py)
  set_tests_properties(qualification:research_profile PROPERTIES LABELS "unit;qualification")
endif()
add_executable(qualification_continuity_floor ../qualification/continuity_floor.f90)
target_link_libraries(qualification_continuity_floor PRIVATE transport_audit multifluid meshobj_cart grid const gemini3d)
intel_fortran_main_linker(qualification_continuity_floor)
if(NUMPY_FOUND AND H5PY_FOUND)
  add_test(NAME qualification:continuity_floor COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_continuity_floor.py --exe $<TARGET_FILE:qualification_continuity_floor>)
  set_tests_properties(qualification:continuity_floor PROPERTIES LABELS "unit;qualification")
endif()

add_executable(qualification_etd ../qualification/etd_probe.f90)
target_link_libraries(qualification_etd PRIVATE calculus const gemini3d)
intel_fortran_main_linker(qualification_etd)
if(Python_Interpreter_FOUND)
  add_test(NAME qualification:etd_oracle COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_etd.py --exe $<TARGET_FILE:qualification_etd>
    --output ${CMAKE_CURRENT_BINARY_DIR}/etd-oracle.json)
  set_tests_properties(qualification:etd_oracle PROPERTIES LABELS "unit;qualification")
  foreach(probe IN ITEMS source_ledger kernel_memory host_capabilities hosted_evidence qualification_dependencies)
    add_test(NAME qualification:${probe} COMMAND ${Python_EXECUTABLE}
      ${PROJECT_SOURCE_DIR}/test/qualification/test_${probe}.py)
    set_tests_properties(qualification:${probe} PROPERTIES LABELS "unit;qualification")
  endforeach()
  add_test(NAME qualification:model_guard COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_mode_guard.py
    --alternate $<TARGET_FILE:gemini.altenergy.bin> --density $<TARGET_FILE:gemini.denspot.bin>)
  set_tests_properties(qualification:model_guard PROPERTIES LABELS "unit;qualification")
endif()
if(NUMPY_FOUND AND H5PY_FOUND)
  foreach(probe IN ITEMS hdf_policy exchange)
    add_test(NAME qualification:${probe} COMMAND ${Python_EXECUTABLE}
      ${PROJECT_SOURCE_DIR}/test/qualification/test_${probe}.py)
    set_tests_properties(qualification:${probe} PROPERTIES LABELS "unit;qualification")
  endforeach()
endif()
if(NUMPY_FOUND AND SCIPY_FOUND)
  foreach(probe IN ITEMS campaign campaign_boundaries)
    add_test(NAME qualification:${probe} COMMAND ${Python_EXECUTABLE}
      ${PROJECT_SOURCE_DIR}/test/qualification/test_${probe}.py)
    set_tests_properties(qualification:${probe} PROPERTIES LABELS "unit;qualification")
  endforeach()
endif()

add_executable(qualification_energy_operators ../qualification/energy_operator_balance.f90)
target_link_libraries(qualification_energy_operators PRIVATE diffusion PDEparabolic meshobj_cart gemini3d)
intel_fortran_main_linker(qualification_energy_operators)
add_test(NAME qualification:energy_operators COMMAND qualification_energy_operators)
set_tests_properties(qualification:energy_operators PROPERTIES LABELS "unit;qualification")
if(Python_Interpreter_FOUND)
  add_test(NAME qualification:energy_ledger COMMAND ${Python_EXECUTABLE}
    ${PROJECT_SOURCE_DIR}/test/qualification/test_energy_ledger.py)
  set_tests_properties(qualification:energy_ledger PROPERTIES LABELS "unit;qualification")
endif()

foreach(audit_extension IN ITEMS numerics lifecycle contracts restart installation)
  include("${CMAKE_CURRENT_LIST_DIR}/${audit_extension}.cmake" OPTIONAL)
endforeach()
